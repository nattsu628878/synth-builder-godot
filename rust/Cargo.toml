[package]
name = "synth_circuit"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "0.4.5", features = ["api-4-4"] }

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
